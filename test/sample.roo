procedure main ()
    integer x, y, temp;
    integer quotient;
    integer remainder;
{
    write "Input two positive integers: ";
    read x;
    read y;
    write "\n";
    if x < y then
    temp
    x <-
    y <-
    fi
    write
    write
    write
    write
    write
    then
    <- x;
    y;
    temp;
    "The gcd of ";
    x;
    " and ";
    y;
    " is ";
    quotient <- x / y;
    remainder <- x - quotient * y;
    while remainder > 0 do
    x <- y;
    y <- remainder;
    quotient <- x / y;
    remainder <- x - quotient * y;
    od
    write y;
    write "\n";
}