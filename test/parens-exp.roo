procedure test ()
{
    n <- (a * b) + c;
    n <- (a / b) + c;
    n <- (a + b) + c;
    n <- (a - b) + c;
    n <- (a * b) - c;
    n <- (a / b) - c;
    n <- (a + b) - c;
    n <- (a - b) - c;
    n <- (a * b) * c;
    n <- (a / b) * c;
    y <- (a + b) * c;
    y <- (a - b) * c;
    n <- (a * b) / c;
    n <- (a / b) / c;
    y <- (a + b) / c;
    y <- (a - b) / c;

    y <- a * (b + c);
    y <- a / (b + c);
    n <- a + (b + c);
    y <- a - (b + c);
    y <- a * (b - c);
    y <- a / (b - c);
    n <- a + (b - c);
    y <- a - (b - c);
    n <- a * (b * c);
    y <- a / (b * c);
    n <- a + (b * c);
    n <- a - (b * c);
    y <- a * (b / c);
    y <- a / (b / c);
    n <- a + (b / c);
    n <- a - (b / c);
    x <- (p - 1) * (q - 1);
x <- ((13+(3*5)*7)-(2*7))-(11/(20/(2*5)));
y <- (3640 + ( (bal-37000) / 3 ) );
}


